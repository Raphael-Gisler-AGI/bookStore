<mvc:View
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:macros="sap.fe.macros"
    controllerName="fpm.ext.main.Main">
    <f:DynamicPage>
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <Title text="{i18n>appTitle}" />
                </f:heading>
                <f:expandedContent>
                    <Label text="{i18n>appDescription}" />
                </f:expandedContent>
            </f:DynamicPageTitle>
        </f:title>
        <f:content>
            <Panel>
                <macros:FilterBar
                    id="BooksFilterBar"
                    metaPath="@com.sap.vocabularies.UI.v1.SelectionFields"
                    contextPath="/Books"
                    liveMode="true" />
                <macros:Table
                    id="BooksTable"
                    metaPath="@com.sap.vocabularies.UI.v1.LineItem"
                    contextPath="/Books"
                    filterBar="BooksFilterBar" />
            </Panel>
        </f:content>
    </f:DynamicPage>
</mvc:View>